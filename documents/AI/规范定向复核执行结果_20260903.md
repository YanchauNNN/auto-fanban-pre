# 规范定向原页复核执行结果

日期：2026-09-03。工作区：`codex-calculation-ai-unified`；分支：`codex/calculation-ai-unified`。

## 提交与推送

上阶段25个修复文件已提交为`0bb22b9`：`fix(ai): validate standards evidence and preserve SHA-based corpus reuse`。提交前重新执行AI测试：456通过、2跳过。

向已配置GitHub origin推送两次（包含一次HTTP/1.1重试），均收到`Recv failure: Connection was reset`；独立HTTPS请求也连接超时。没有修改全局Git/网络配置、没有强推，不能认为已推送成功。本地分支显示领先远端跟踪分支11个提交。

本次后续新增的复核流程、测试、转录数据与文档尚未再次提交。执行期间工作区出现了其他任务的前后端/任务流程改动，本任务没有提交、回退或修改它们。`.t/`临时文件和`storage/`数据不纳入Git。

## 进度与目标回顾

最终目标仍是随程序部署、完全离线检索的规范skill：开发MiniMax与终端内网Qwen共用后端证据，回答可以追溯到具体版本、条款、物理页、印刷页及表格。解析入库不是训练模型，也不等于全部原文已准确。

上阶段保留了504份PDF的解析成果，但4个独立原页反例尚未通过。因而本阶段先完成小范围的“原页复核、更正、重放、验收”流程，不启动其余OCR队列，也不替换正式运行库。

计划见[定向复核计划](plans/2026-09-03-standards-reviewed-evidence.md)。

## 本轮完成

视觉核对6个实际PDF页面，包括两条跨页条款的续页：

| 来源 | 范围 | 更正 |
|---|---|---|
| GB 50223-2008 | 第1.0.1条，PDF第7页 | 独立条款正文、印刷页1，不包括相邻条款 |
| GB 50367-2013 | 第3.1.7条，PDF第22-23页 | 完整5款、跨页衔接，去掉混入的页脚文字，印刷页7-8 |
| GB 50098-2009 | 表3.2.2，PDF第15页 | 完整多层列标题、18个数值单元格、单位及2条附注，印刷页7 |
| GB 50098-2009 | 第4.1.3条，PDF第16-17页 | 完整3款，将原页确认的`1000m²`与旧OCR的`1000m°`区分，印刷页8-9 |

复核者记录为Codex原PDF视觉转录，不冒充人工工程审签。图像证据位于`storage/ai/standards-reviewed-20260903/evidence/`。

实现文件：

- `tools/ai/building-structure-standards/scripts/standards_reviews.py`：严格校验和记录级复核重放、查询时完整性校验。
- `scripts/apply_evidence_reviews.py`：显式输入/输出的离线入口，不允许原地覆盖输入库。
- `assets/data/reviewed_evidence_20260903.json`：4份完整转录记录，与原独立验收集分开维护。
- `scripts/standards_query.py`：返回精确复核范围，分开内容质量与设计建议资格。
- 后端skill提示同步解释`record_review`，仍以`design_advice_allowed`约束最终设计建议。

更正绑定PDF SHA256、标准号、记录编号、物理页范围、原记录指纹、全部续页、印刷页、复核者、时间和渲染图哈希。不同PDF、旧文本变化、页缺失、范围不符或重复更正会拒绝发布；运行时记录或复核元数据变化也不能继续使用原复核结论。

更正前完整记录保存在`evidence_reviews.original_record_json`；原生/OCR页面完全不变。更正后的条款同时更新普通检索与FTS索引。复核仅作用于目标记录，不把整页或整本标记为合格。

`quality_status=usable`仅描述内容转录质量；标准状态、授权、正文角色和其他证据仍独立限制`design_advice_allowed`。这3个标准的状态仍保持待核验，不能因此输出最终设计依据。

## 实际验证

| 项目 | 结果 |
|---|---|
| 完整AI回归 | 475通过、2跳过 |
| 新增复核模块定向测试 | 20通过，含之后补充的真实打包/解包安装用例；其余19项已包含在完整AI回归中 |
| 终端部署构建测试 | 38通过 |
| 原4项独立原页验收 | 4/4通过，`independent_content_passed_count=4`，表格不是仅定位通过 |
| 未更正候选库的原安全反例 | 6/6通过，旧OCR风险仍被拒绝放行 |
| 更正后全库结构检查 | 7/7通过 |
| 旧15本76项原样冒烟 | 72/76；4项仍为两个标准的状态/警告口径差异，没有改写旧答案 |
| Ruff / 本任务diff检查 | 通过 |

两个跳过项均为当前Windows环境不支持创建符号链接的既有测试。本轮这些测试未再出现上阶段的大联跑进程崩溃，但不据此宣称该异常根因已查清。

打包验证使用合成小样本，实际调用现有打包器、解包安装函数和后端查询子进程，确认`standards_reviews.py`与SQLite审计记录一起携带，PDF未入包；没有安装正式skill或制作实机发布包。没有前端修改、未运行前端测试、没有调用外部模型或实机模型。

## 候选库与资产保护

更正后候选库：`storage/ai/standards-reviewed-20260903/standards.sqlite`。

- 504份来源、50,410页、158,684条条款、59,559个表格记录仍保留。
- 4份复核匹配5条记录，其中GB 50223同SHA有2份目录副本。
- 与更正前候选库逐条核对：全部50,410页和来源元数据不变；仅4条条款记录、1张表格记录变化，并有5条审计记录。
- 原运行库、原增量状态、15本基线和更正前候选库SHA均未变化。
- 没有执行任何新OCR，没有删除旧缓存，没有重启服务，没有修改main。

| 文件 | SHA256 |
|---|---|
| 更正前候选数据库 | `33c813484ac8015133916f1cd6582feadd3a3c6b369b0e38d4f31c9b57b55115` |
| 更正后候选数据库 | `494d294289116ddf14d09beb6130f571a74039539deadeed20258a63ab58a9c0` |
| 复核转录清单 | `f5e2da923c313846a15fc8c8fb44d852babb9ef3559b512dd26a39a27e577131` |
| 原独立验收集，未修改 | `fa66551debb32f41c55c538cb39e5b0624d4d373ba8218e330ad307e6373b736` |

该目录中的`replay_report.json`、`reference_validation.json`、`structure_validation.json`、`unreviewed_safety_validation.json`和`pilot_smoke_validation.json`保留机器可读结果。代码测试XML位于`.t/standards-reviews-*.xml`。

## 下一阶段

这4项是已知错误的定向回归，不是未见过样本上的准确率测试，也不代表15本质量基线全面合格。

下一阶段应为剩余试点规范补独立原页样例，覆盖未参与更正的条款、表格、公式和附图，先建立覆盖15本的分层验收。随后按PDF SHA256复用旧OCR，只对缺失或风险区域做增量识别。

全库仍有83份来源没有识别到条款、7,306页低文本；本阶段未处理。官方状态口径、项目适用版本和更多图表数值仍需独立确认。正式发布前还要完成全量质量覆盖统计、真实模型问答以及实机部署验收。
