# 项目更新日志

## 2026.01.04.1：规范与模板起步

### 阶段目标

项目最初并不是从界面开始，而是先把“要生成什么、参数从哪里来、模板落到哪里、图纸信息如何识别”这几件基础问题说清楚。这个阶段的核心目标，是把程序从一个想法固定成一套可验证的工程规则。

### 代表提交

- `cd4a489`：初始仓库，建立项目最早的文件基座。
- `2dfa53c`：形成早期参数规范，将文档生成参数和图签识别配置合并到同一套 YAML 事实源中。
- `944b05d`：补充 Office 模板、DXF 标准样例和模板扫描工具。
- `b461a28`：继续补充参数表、遗漏检查和运行所需的基础资料。

### 实际代码与文档变化

- `documents/参数规范.yaml` 开始承担“事实源”角色，早期版本已经明确区分 `doc_generation_spec` 和 `titleblock_extraction_spec`，避免把业务表单、模板落点、图签 ROI 配置混在同一层。
- `documents/参数表.md`、`documents/遗漏检查.yaml`、`tools/工具列表.yaml` 提供了人能读懂的业务参数说明和检查清单。
- `documents_bin` 中逐步放入封面、目录、IED、设计文件等模板，程序目标从一开始就绑定真实交付物，而不是只做演示数据。
- `dxf`、`dxf-biaozhun` 和模板扫描脚本为后续 CAD 识别、字段抽取、模板落点验证提供了样例基础。

### 成果意义

这一阶段的价值在于先建立“规范驱动”的项目骨架。后续前端表单、后端校验、文档生成、字体治理和翻版配置，都能回到 YAML 与模板资产中找依据，减少了靠口头记忆维护程序规则的风险。

## 2026.01.19.1：CAD 后端主链成型

### 阶段目标

这个阶段开始把规范真正变成可运行的后端流水线。程序需要完成 DWG/DXF 输入、图框检测、图签字段提取、A4 多页识别、图纸拆分、文档生成和最终打包，形成“从图纸到交付物”的主流程。

### 代表提交

- `7a1fbdd`：创建 `backend` 工程结构，搭建 CAD、配置、文档生成、模型和 pipeline 模块。
- `489c291`：补充图框检测、A4 多页、图签提取和文档生成相关实现。
- `fd21d69`：继续完善检测链、任务执行器和运行期配置。
- `4940fed`：推进图框定位、候选框筛选和流水线测试。

### 实际代码与文档变化

- `backend/src/pipeline/executor.py` 建立了交付任务的主执行器，阶段包括落盘、转换、检测图框、抽取字段、A4 成组、拆图、导出、生成文档和打包。
- `backend/src/cad/frame_detector.py`、`backend/src/cad/detection/candidate_finder.py`、`backend/src/cad/detection/anchor_first_locator.py` 让图框识别从单点规则走向候选筛选和锚点校准。
- `backend/src/cad/a4_multipage.py` 与 `backend/src/models/sheet_set.py` 让 A4 多页图纸可以作为成组对象参与后续文档和 PDF 输出。
- `backend/src/doc_gen/catalog.py`、`backend/src/doc_gen/cover.py`、`backend/src/doc_gen/design.py`、`backend/src/doc_gen/ied.py` 开始把提取到的图签和用户参数写入真实 Office 模板。

### 成果意义

项目从“规范和模板资产”进入“可执行后端流水线”。这个阶段确定了程序的主能力：自动识别图册中的结构图纸信息，并把它们转换成目录、封面、设计文件、IED 计划和打包产物。

## 2026.02.25.1：Module5 与终端运行版

### 阶段目标

早期 Python/DXF 处理链可以跑通核心逻辑，但真实工程出图还需要更稳定地调用 AutoCAD 能力。这个阶段围绕 Module5 展开，重点是 CAD 打印、窗口选择、PC3/PMP 资源、.NET Bridge 和可交付的简易终端包。

### 代表提交

- `c7e8621`：增强 CAD 处理，推进 Module5 能力。
- `61187b3`：增加对 `.NET Bridge` 引擎的支持，把 CAD 执行能力封装成桥接层。
- `2623aa7`：简易 exe 可用，终端运行链路开始具备实际交付形态。
- `a0a6379`：修复简易 exe 的打印边缘问题并重新打包。

### 实际代码与文档变化

- `backend/src/cad/accoreconsole_runner.py` 负责调度 AccoreConsole 与 CAD 脚本，成为后端进入真实 AutoCAD 环境的重要入口。
- `backend/src/cad/cad_dxf_executor.py` 承担 CAD/DXF 执行编排，支持切图、打印和导出阶段的任务分发。
- `backend/src/cad/dotnet/Module5CadBridge/Commands.cs`、`PlotEngine.cs`、`SelectionEngine.cs` 把 `split_only`、`plot_window_only`、`plot_from_split_dwg` 等工作阶段交给 .NET Bridge 执行。
- `backend/src/cad/scripts/module5_bootstrap.scr` 和 `module5_cad_executor.lsp` 为 AutoCAD 批处理和桥接启动提供脚本入口。
- `test/dist/src/fanban_m5_launcher.py` 等终端产物让模块 1~5 可以脱离开发环境进行简易运行和验证。

### 成果意义

程序开始从“后端算法可跑”走向“工程环境可出图”。通过 Module5 和 .NET Bridge，项目把 CAD 运行时的不确定性封装起来，为后续网页提交任务、服务器排队、终端部署打下基础。

## 2026.03.08.1：API 与前端工作台版

### 阶段目标

这个阶段的核心变化，是把本地脚本能力搬到网页工作台。用户不再只通过命令行或终端包触发程序，而是可以通过 API 和前端页面上传 DWG、填写参数、创建任务、查看状态并下载产物。

### 代表提交

- `a67fee2`：新增 Module7 deliverable API，建立 `/api/jobs` 任务接口。
- `3771209`：创建前端工作区，使用 Vite/React 搭建初版交付处理页面。
- `ffcbbe9`：完成前端联调与任务结果修正，补齐 API 地址、代理和任务详情适配。
- `948c6a1`：整合前端工作台与文档交付链路，让网页入口和后端交付主链对齐。

### 实际代码与文档变化

- `API/app/routers/jobs.py` 增加任务创建、任务列表、任务详情、下载包和下载 IED 等接口。
- `API/app/runtime.py` 建立运行时服务，负责任务校验、上传落盘、任务排队、状态查询和产物路径管理。
- `frontend/src/app/App.tsx` 搭建任务工作台和任务记录区域，开始承载真实任务状态。
- `frontend/src/features/deliverable/DeliverableWorkspace.tsx` 根据后端 schema 渲染交付表单，支持 DWG 上传、字段校验、提交任务和错误回显。
- `frontend/src/platform/api/httpAdapter.ts` 把前端表单和后端接口连接起来，形成浏览器到 FastAPI 的真实提交链路。

### 成果意义

项目从“后端处理工具”升级为“可交互的网页工作台”。这一步很关键，因为后续纠错、翻版、字体预检、PDF 预览和任务组展示，都是在这个 API/前端底座上继续扩展出来的。

## 2026.03.13.1：纠错、翻版与字体替代版

### 阶段目标

在交付出图之外，项目开始加入更贴近实际工程审查和复用场景的能力：纠错检查、翻版替换、字体预检与替代、出图联动。同时，后端也开始建立任务组、账号、流程、工作量、归档等管理类基础能力。

### 代表提交

- `ab5f976`：翻版功能第一次提交，同时引入纠错扫描、词库、匹配和报告能力。
- `f425d6a`：纠错落入前端第一版，新增纠错配置页面和摘要展示。
- `315b4db`：接入翻版前端和字体替代，补充 `preflight-fonts` 与缺失字体处理链。
- `fd1785d`：修复前端教程，并在后端更新三大业务板块。

### 实际代码与文档变化

- `backend/src/audit_check` 中的 `executor.py`、`matcher.py`、`lexicon.py`、`reporting.py` 形成纠错检查主链，支持词库扫描、归一化匹配、区域映射和报告输出。
- `backend/src/audit_replace` 开始承载翻版任务，后续逐步从文字替换扩展到更复杂的图纸块替换。
- `frontend/src/features/audit-check/AuditCheckWorkspace.tsx` 把纠错配置落到前端，用户可以选择项目号、上传 DWG 并创建真实纠错任务。
- `frontend/src/features/replace/ReplaceWorkspace.tsx` 增加翻版配置入口，支持“仅翻版”和“同步出图和翻版”的交互雏形。
- `backend/src/cad/font_preflight.py`、`font_inventory.py`、`font_preflight_bridge.py` 让程序能在正式出图前检查缺失字体，并向前端返回替代建议。
- `backend/src/accounts`、`backend/src/task_groups`、`backend/src/workflow`、`backend/src/workload`、`backend/src/archive` 建立管理类后端基础；这些能力具备接口和服务层基础，但前端深度管理页面仍需要继续完善。

### 成果意义

项目从单一“自动出图”扩展为“出图 + 纠错 + 翻版 + 字体治理 + 管理底座”的综合工作台。它开始覆盖工程交付中更真实的痛点：发现图纸文本问题、批量替换项目号、处理字体缺失、把任务组织成可追踪的工作流。

## 2026.03.25.1：稳定性与字体治理版

### 阶段目标

这个阶段的重点从“功能接入”转向“稳定运行”。项目开始系统治理字体问题、提升图框检测性能、提高 CAD 并发槽位、补充非 A4 同编码多页处理，并通过交接文档把运行经验沉淀下来。

### 代表提交

- `b36d16d`：优化图框检测性能，并将 CAD 默认槽位提升至 8。
- `e91c5a5`：修复字体问号问题，新增合并非 A4 图纸处理。
- `4db51b2`：完善字体默认映射与文本提取修复。
- `a5ef9d7`：补充后端工作交接说明和字体库目录维护说明。

### 实际代码与文档变化

- `backend/src/config/runtime_config.py` 将 `cad_runtime.slot_count` 提升到 8，并让并发参数、CAD 运行参数和字体配置进入运行期配置。
- `backend/src/cad/detection/candidate_finder.py`、`anchor_first_locator.py`、`anchor_calibrated_locator.py` 继续优化候选框搜索、A4 簇统计和锚点优先策略，减少大图册识别压力。
- `backend/src/cad/same_code_multipage.py` 识别非 A4 的同编码多页族，避免只把多页问题限定在 A4 场景。
- `backend/src/cad/font_mapping_runtime.py`、`font_preflight.py`、`font_replacement_plan.py` 把字体缺失处理从临时替换升级为运行期映射、类型兜底和二次预检。
- `documents/字体库目录维护说明.md` 明确了字体处理优先级：优先命中原字体，再走精确映射，最后才按字体类型兜底。
- `documents/后端工作交接总说明.md` 将后端模块边界、运行链路和维护注意事项整理成可交接资料。

### 成果意义

这个阶段让项目更像一个可长期维护的工程系统，而不是一次性的脚本集合。字体库、并发槽位、非 A4 多页、图框性能和交接文档共同提升了程序在真实图册上的可用性和可延续性。

## 2026.04.09.1：出图质量与 PDF 预览版

### 阶段目标

4 月中下旬集中处理真实使用中暴露的问题：图框偏位、字段提取、目录命名、IED 选填、标题规范、PDF 预览、打包部署和本地运行产物管理。这个阶段的关键词是“收束”和“可用性”。

### 代表提交

- `bde69e6`：大量修复图框偏位、字段提取和工种负责人非必选问题。
- `b8ba3c7`：更新 IED 计划选填和标题规范。
- `5404ea2`、`0923d40`：增加前端 PDF 可视化和网页 PDF 预览能力。
- `5700669`：整理部署构包与本地运行产物忽略。

### 实际代码与文档变化

- `backend/src/pipeline/frame_filtering.py`、`backend/src/cad/titleblock_extractor.py`、`backend/src/pipeline/shared_prep.py` 持续修复图框筛选、字段提取和共享预处理逻辑。
- `backend/src/models/doc_context.py` 和 `backend/src/doc_gen` 相关模块进一步调整 IED、目录、标题和文档生成上下文，让可选字段不再强行阻断任务。
- `backend/src/pipeline/preview_pdf_service.py` 支持把导出的图纸 PDF 合并成预览 PDF，并在纠错场景中生成带标注的预览结果。
- `frontend/src/app/App.tsx` 接入 `react-pdf` 预览弹窗，任务详情中可以打开普通 PDF 预览或纠错标注 PDF。
- `API/app/routers/jobs.py` 和 `API/app/runtime.py` 增加 `download/preview` 以及 `preview_available`、`preview_mode`、`preview_download_url` 等结果字段。
- `backend/src/deploy/terminal_package.py` 和 `.gitignore` 继续整理部署构包、前端构建产物和本地运行痕迹，降低交付包混入临时文件的风险。

### 成果意义

这个阶段让程序从“能跑完任务”进一步走向“用户能看懂结果、能预览问题、能下载正确产物、能部署运行”。PDF 预览尤其重要，它把后台出图结果重新带回前端，让用户不必只依赖压缩包下载来判断任务质量。

## 2026.05.07.1：厂房索引图翻版增强版

### 阶段目标

最新阶段继续强化纠错和翻版。项目不再只做普通文本替换，而是开始处理厂房索引图这类更接近图纸语义的块级替换，并支持来源/目标项目的机组、岛号差异。

### 代表提交

- `ba8653e`：改进 CAD 纠错扫描和归一化匹配，同时修复翻版执行器相关问题。
- `5009631`：厂房索引图替换基础算法完成。
- `5330ac0`：配置多目标厂房索引图模板。
- `70d80c7`、`2ebcf1b`：修复替换后 DWG 命名，并区分来源厂房索引图变体。
- `4abcfcb`：更新翻版功能，将最新字段和前端交互继续同步。

### 实际代码与文档变化

- `backend/src/audit_replace/factory_index_maps.py` 新增厂房索引图候选识别、模板锚点提取、替换计划构建和缩放对齐逻辑。
- `backend/src/audit_replace/factory_index_bridge.py` 将厂房索引图替换接入 CAD Bridge，生成任务 JSON、结果 JSON 和替换报告。
- `backend/src/audit_replace/executor.py` 把厂房索引图替换纳入翻版主链，替换结果会写入 `progress.details.factory_index_map`。
- `backend/src/cad/dotnet/Module5CadBridge/FactoryIndexMapReplacer.cs` 承担 AutoCAD 侧的实际块替换执行。
- `documents/参数规范.yaml` 和 `documents/参数规范_运行期.yaml` 增加厂房索引图模板映射、来源变体字段、目标变体字段和项目校验规则。
- `documents/厂房索引图替换前端接入指导.md` 明确前端只需要提交 `source_island_no` 与 `target_island_no`，不需要读取模板文件、解析 DWG 或实现块替换。
- `frontend/src/features/replace/ReplaceWorkspace.tsx` 已支持来源项目和目标项目的机组/岛号选择，并在提交时把 `source_island_no`、`target_island_no` 传给后端。

### 成果意义

翻版能力从“项目号和文本替换”迈向“图纸内容结构替换”。对于 `2016`、`1916` 这类存在机组或岛号差异的项目，程序已经能通过来源和目标变体规则选择正确模板，并把厂房索引图替换作为翻版主链的一部分执行。

### 当前能力边界

- 交付出图、纠错检查、翻版替换、字体预检、PDF 预览和任务下载已经形成前后端贯通链路。
- 厂房索引图替换已经接入翻版后端主链，前端已补充来源/目标机组或岛号字段，但块替换规则本身仍应保持在后端和 YAML 中维护。
- 账号、任务组、流程、工作量、归档等管理模块已经具备后端服务和接口基础，前端仍需要继续补齐面向日常管理的完整页面和权限体验。
- 后续最值得继续投入的方向，是把稳定性验证、前端管理页面、部署运维说明和真实样本回归测试继续体系化。

## 2026.05.26.1：机组纠错、出图环境与前端维护版

### 阶段目标

这个阶段的重点不再是单独新增一个入口，而是继续围绕真实部署和真实图纸反馈补齐细节。纠错需要识别项目号和机组号是否一致，出图需要稳定处理钢衬里、字体、PDF 打印路径和 Office 导出问题，前端则需要让出图、纠错、翻版、预览和字体处理在同一工作台里更加顺手。

### 代表提交

- `ccce575`（2026-05-14，最新修改，修复bug）、`70eb89d`（2026-05-21，修复多类bug）：修复纠错、图签提取、非 A4 多页、目录生成和前端交互中的多类问题。
- `e4b2467`（2026-05-17，更新）：继续强化 PDF 预览、升版标记、目录页数和前端交付工作台联动。
- `64fb320`（2026-05-18，更新钢衬里反馈）：根据钢衬里反馈补充 `结构二室大图.ctb`、字体库和预览标注处理。
- `eb6fe63`（2026-05-19，修复排版bug）、`fec92eb`（2026-05-19，前端维护，IED文件更新）、`098ea86`（2026-05-20，最新更新-电力工程研究设计所适配）：维护前端排版细节，更新 IED 模板，并适配电力工程研究设计所相关出图规则。
- `060e39d`（2026-05-20，更新仅拆图）：补充“仅拆图”任务链路，让任务可以只完成 DWG 拆分而不强制进入完整文档交付。
- `b6ee037`（2026-05-22，修复打印时字体位置偏移的问题）、`f1b6f0e`（2026-05-22，补全上一次修复）、`981fd48`（2026-05-26，打印方式回退原图打印，并增加regen命令）：修复 PDF 打印字体位置偏移，将主打印路径回退到原始 DWG 窗口打印，并在出图前增加 `REGEN` 处理。
- `c1ef415`（2026-05-25，更新机组号纠错）、`937478f`（2026-05-25，第一次补全机组号纠错的错误）：补齐机组号纠错、文件名自动推断、CAD 环境指纹和同步脚本。
- `692a5dc`（2026-05-26，深化后端关闭时前端提醒的时间检测间隔）：调整前端健康检查和后端关闭提示，让维护状态、任务状态和 Office 导出异常显示更清楚。

### 实际代码与文档变化

- `backend/src/audit_check/matcher.py` 和 `backend/src/audit_check/executor.py` 增加项目号 + 机组号一致性检查，既能识别完整内部编码，也能识别“X号机组 / X号岛”和同图册中出现过的短厂房码，避免把普通尺寸文本误判成机组问题。
- `backend/src/pipeline/project_no_inference.py`、`API/app/runtime.py` 和前端上传推断逻辑开始从 DWG 文件名中推断项目号、机组号，并把 `unit_no` 作为纠错和交付参数继续传递。
- `documents/参数规范.yaml` 和 `documents/参数规范_运行期.yaml` 增加 `1915` 项目、`unit_no` 字段、机组一致性规则、字体兼容模式映射和原始 DWG 窗口打印开关，继续把机制参数放回 YAML 事实源中维护。
- `tools/cad_env_fingerprint.ps1` 和 `tools/cad_env_sync.ps1` 建立 CAD 环境差异化检查与同步的脚本基础，用于对比开发机和部署机的字体、PC3/PMP/CTB、Support Path、Profile 和 AcCoreConsole 实际加载环境。
- `backend/src/cad/dotnet/Module5CadBridge`、`backend/src/cad/cad_dxf_executor.py` 和 `backend/src/cad/scripts/module5_cad_executor.lsp` 继续修复 PDF 打印链路，补充字体兼容模式、原图窗口打印、打印前重生成和团队 SHX/TTF 字体库接入。
- `backend/src/doc_gen/catalog.py`、`backend/src/doc_gen/pdf_engine.py` 和 `backend/src/pipeline/executor.py` 强化目录页数、Excel PDF 导出重试和导出失败分类，让 Office COM 暂时不可用、目录 PDF 失败和 CAD 导出失败能被更清楚地区分。
- `frontend/src/app/App.tsx`、`PreviewPdfModal.tsx` 和 `frontend/src/features/deliverable/DeliverableWorkspace.tsx` 继续调整工作台交互，补齐 PDF 预览、仅拆图入口、字体替换查看、字体兼容模式、草稿清理和后台健康状态降级提示。
- `documents/终端实装安装计划.md` 和 `documents/CAD环境差异化检查与同步脚本任务书.md` 把离线部署、IIS 代理、Office COM、运行环境检查、CAD 环境指纹和部署机同步思路整理为后续实装依据。

### 成果意义

项目开始从“功能可用”进一步走向“现场可解释、可部署、可回归”。机组号纠错让程序能发现同一项目下 1/2/3/4 号机组或岛号混用的问题；CAD 环境指纹把“开发机正常、部署机异常”的出图差异转成可检查项；原始 DWG 窗口打印、字体兼容模式和 `REGEN` 处理则继续压低 PDF 字体变形、偏移和缺字风险。

### 当前能力边界

- 交付、纠错、翻版、仅拆图、字体预检、字体替换、PDF 预览和任务下载已经处在同一个前后端工作台链路中，但真实部署机上的 AutoCAD / Office 环境仍需要通过指纹脚本和样本烟测继续确认。
- 机组号纠错已经具备后端规则、YAML 配置和前端字段基础，后续应继续用真实项目图册验证误报和漏报边界，尤其是短厂房码和非 JGS 图册类型。
- 打印链路目前重新偏向原始 DWG 窗口打印，并保留字体兼容和打印前 `REGEN` 兜底；如果部署机仍出现 PDF 字体异常，应优先检查 Profile、字体路径、PC3/PMP/CTB 和 Support Path，而不是只看 DWG 本身。
- 前端管理类模块仍不是本阶段完成重点，账号、流程、工作量和归档的日常管理页面还需要继续在既有模块页签下补齐。

## 2026.05.26.2：第三份参数规范与机制参数落盘版

### 阶段目标

这个阶段的重点，是继续收紧“后端变量参数必须回到 YAML 事实源”的原则。前两份 YAML 已经分别承载业务规则和运行期配置，但账号权限、归档兜底、工作量结算、纠错展示、项目号推断、API 阶段显示、CAD/Office/部署机制等后端机制参数仍然分散在代码中。为了不继续拉长既有两份规范，本次新增第三份参数规范，专门承接这些机制类参数，并让后端在实际使用点读取它。

### 代表提交

- `c8f9daf`（2026-05-27）：整理了最新的翻版内容和检查脚本。将第三份参数规范、机制参数加载器、归档身份解析、权限/结算/纠错展示读取点和终端部署复制逻辑落为正式提交。

### 实际代码与文档变化

- `documents/参数规范-3.yaml` 新增 `backend_mechanism` 根域，集中记录权限门禁、归档兜底值、工作量结算策略、纠错展示优先级、项目号推断规则、API 阶段显示、CAD 运行机制和部署机制参数，避免和 `参数规范.yaml`、`参数规范_运行期.yaml` 的既有根域重复。
- `backend/src/config/mechanism_spec.py` 新增第三份规范加载器，支持默认路径和 `FANBAN_MECHANISM_SPEC_PATH` 覆盖；同时禁止重复声明已有 YAML 根域，并在能找到业务规范时校验权限角色必须属于 `management_features.account.valid_roles`。
- `documents/参数规范_运行期.yaml` 增加 `paths.mechanism_spec_path` 和启动期环境变量说明，但 `RuntimeConfig.from_yaml()` 只解析路径，不在加载运行期配置时强制读取第三份 YAML，避免最小运行期规范被第三份文件缺失拖垮。
- `API/app/routers/accounts.py`、`API/app/routers/workload.py`、`backend/src/task_groups/visibility.py`、`backend/src/workflow/visibility.py` 将账号、工作量和流程可见性中的角色门禁改为读取 YAML；其中任务可见性继续以 `参数规范.yaml` 的业务角色范围为准，不重复搬迁。
- `backend/src/archive/identity.py` 抽出统一归档身份解析，`ArchiveService` 和 `TaskGroupSubmitGuards` 共用同一套 `level_pattern + archive_defaults`，避免提交前冲突检测和最终归档目录口径不一致。
- `backend/src/workload/settlement_service.py`、`backend/src/audit_check/matcher.py`、`backend/src/audit_check/reporting.py`、`backend/src/result_views.py`、`backend/src/pipeline/project_no_inference.py` 和 `API/app/runtime.py` 分别接入工作量结算、纠错禁词与分组、项目号推断、阶段显示和作业完成等待超时等机制参数。
- `backend/src/cad`、`backend/src/deploy`、`test/dist/src/fanban_m5_launcher.py`、`tools/probe_target_env.ps1` 和 `backend/tests/management_test_helpers.py` 同步第三份规范路径与部署复制逻辑，确保本地、测试、探测脚本和终端部署包使用同一套参数来源。
- `backend/src/doc_gen/derivation.py` 顺手修正无 `-001` 首页时的图册基准码派生，避免封面和目录编码继承 `-003` 这类图纸序号；`backend/src/doc_gen/cover.py` 将 Word/Excel COM 限流上下文拆回显式嵌套写法，保持现有静态守卫测试可读。

### 成果意义

这次变动不是单纯新增一份文档，而是把第三份 YAML 真正接入后端读取链路。项目现在形成了更清晰的三层事实源：`参数规范.yaml` 继续管业务规则，`参数规范_运行期.yaml` 继续管运行期环境，`参数规范-3.yaml` 管后端机制策略。后续再发现新的机制类硬编码时，应优先追加到第三份规范并接入读取，而不是继续把默认值留在服务代码里。

### 当前能力边界

- `titleblock_overrides.*` 仍属于运行期规范中已经存在但尚未完全接入读取的既有问题，本次没有迁入第三份 YAML；后续应单独决定是补齐读取链路，还是从运行期规范中移除。
- `plot_resource_manager.py` 中部分资源名仍与运行期 YAML 的 Plot Assets 配置存在代码默认值兜底，实际运行会优先走运行期配置；如果继续推进“零硬编码兜底”，这部分可以作为下一轮专项整理。

## 2026.05.30.1：机组纠错、翻版与前后端连接收束版

### 阶段目标

这个阶段延续 5 月底真实图纸反馈后的修复思路，重点不是再增加一套独立入口，而是把机组纠错、翻版参数下发、前后端连接状态、封面模板和前端细节继续收束到同一条可用链路中。前端要能拿到后端真实能力开关，后端要能按 YAML 和模板资产稳定执行，用户看到的任务状态也需要更接近实际运行状态。

### 代表提交

- `6e8bdb3`（2026-05-28）：完善机组纠错与YAML配置下发。
- `fd087ec`（2026-05-28）：前端小细节修复。
- `b2c4a6e`（2026-05-28）：修复翻版bug。
- `97ffbf1`（2026-05-29）：更新前端后端连接方式。
- `a0c48ff`（2026-05-29）：修复封面绿标以及深化前后端链接。
- `cece419`（2026-05-30）：chore: ignore local debug artifacts。

### 实际代码与文档变化

- `API/app/metadata.py`、`API/app/runtime.py` 和前端 schema/http 适配继续补齐能力下发，让交付、纠错、翻版页面可以从后端读取当前支持的项目、任务开关和运行能力，而不是只依赖前端写死选项。
- `backend/src/audit_check/matcher.py` 继续完善机组一致性检查，结合运行期 YAML 中的规则和项目推断结果，减少短编码、机组号和岛号混用时的误判与漏判。
- `backend/src/audit_replace/executor.py` 强化翻版任务参数校验和厂房索引图替换链路，配合 `frontend/src/features/replace/ReplaceWorkspace.tsx` 的字段展示与提交逻辑，让来源/目标项目配置更稳定地下发到后端。
- `backend/src/doc_gen/cover.py` 和多份 `documents_bin` 封面模板修复绿标、封面版本和不同图册类型的模板落点，降低封面生成时靠后处理补丁兜底的风险。
- `frontend/src/app/App.tsx`、`DeliverableWorkspace.tsx`、`AuditCheckWorkspace.tsx` 和相关测试继续整理工作台状态、任务详情、按钮文案和错误回显，让后端关闭、任务失败、翻版结果和纠错结果的展示更一致。
- `.gitignore` 继续收纳本地调试痕迹和验证截图，避免临时产物进入后续部署包或业务提交。

### 成果意义

这一版把 5 月底分散的真实反馈收成一条更稳定的工作台链路：后端能力可以下发，前端按后端状态展示，机组纠错和翻版不再只是单点功能，封面模板和调试产物也同步整理。它的价值在于减少“本地能跑、页面不清楚；后端有能力、前端没同步”的断层。

### 当前能力边界

- 前后端连接方式已经比上一阶段更清晰，但真实部署机上的 IIS/ARR 代理、后端进程和业务健康仍需要分层检查，不能只看浏览器页面是否能打开。
- 机组纠错和翻版配置已经继续向 YAML 收束，但新增项目或新增模板时，仍应同步检查 `documents_bin`、前端选项和测试夹具。

## 2026.06.23.1：Office 模板、字体替换与规范检查增强版

### 阶段目标

6 月的主要工作是把出图后半段继续做稳。程序不仅要能拆图和打印，还要处理 Excel/Word 模板、PDF 导出、A1 张数统计、字体替换、空字体兼容、DXF 复用和规范检查。这个阶段的目标，是让 Office COM、CAD 字体和业务规范检查在真实样本上更少互相拖累。

### 代表提交

- `2c99ee7`（2026-06-01）：修复excel模组。
- `888e3e5`（2026-06-02）：更新新的图幅支持。
- `7179883`（2026-06-03）：修复word封面。
- `460bcdb`（2026-06-03）：excel打印问题修复。
- `0d6d17e`（2026-06-08）：修复两个问题。
- `51dd98d`（2026-06-09）：修复新业务加噪声。
- `4da08d4`（2026-06-12）：修改 结构: "Structural"。
- `0669225`（2026-06-15）：字体替换功能。
- `d770c2d`（2026-06-16）：优化空字体兼容与DXF复用。
- `ae2ef1e`（2026-06-17）：扩展功能。
- `ddded5f`（2026-06-17）：修复翻版功能的遗漏。
- `6d4bd5a`（2026-06-22）：完善A1张数统计。
- `ad3d06a`（2026-06-23）：更新规范检查。

### 实际代码与文档变化

- `backend/src/doc_gen/pdf_engine.py`、`catalog.py`、`cover.py` 和 `pipeline/executor.py` 继续修复 Excel/Word 导出、目录 PDF、封面模板和任务失败分类，让 Office 侧异常能更准确地回到任务状态中。
- `backend/src/cad/plot_resource_manager.py`、`plot_asset_validation.py`、`cad_dxf_executor.py` 和运行期 YAML 增加新图幅、打印资源和 PC3/PMP/CTB 校验，减少部署环境中缺资源才暴露的问题。
- `backend/src/cad/font_preflight.py`、`font_preflight_bridge.py` 和 .NET Bridge 的 `FontPreflightProcessor.cs` 接入字体替换、空字体兼容和二次预检，让字体问题从“打印后发现”前移到任务准备阶段。
- `backend/src/cad/titleblock_extractor.py`、`a4_multipage.py`、`same_code_multipage.py`、`splitter.py` 继续处理新业务样本带来的噪声、图族统计和图签抽取边界。
- `backend/src/workload/calculator.py`、`backend/src/job_diagnostics.py` 和前端任务展示补齐 A1 张数、诊断信息和工作量相关摘要，为后续工作量结算继续积累基础数据。
- `backend/src/audit_check/standard_review.py`、`matcher.py`、`reporting.py` 和 `documents_bin/规范库.xlsx` 扩展规范审查能力，并把规范检查结果继续接入前端状态和报告输出。
- `documents/参数规范.yaml`、`documents/参数规范_运行期.yaml`、`documents/参数规范-3.yaml` 按业务、运行期、机制三类继续补齐字段，保持“变量参数落盘”的维护原则。

### 成果意义

这一版把交付链路中最容易受环境影响的部分继续前移治理：字体在预检阶段暴露，Office 导出失败能被清楚分类，图幅和打印资源进入运行期配置，规范检查也开始和真实词库、报告输出接上。程序对真实项目样本的容错能力明显增强。

### 当前能力边界

- Office COM 和 CAD 打印仍依赖本机环境，单元测试只能证明编排和参数传递正确，不能替代真实 AutoCAD/Office 样本烟测。
- 字体替换已经具备主链能力，但新增字体库、替代规则或客户机器字体差异仍需要通过预检结果和实际 PDF 输出共同确认。

## 2026.07.13.1：部署队列、连接诊断与运行稳定版

### 阶段目标

7 月上旬的重点转向部署运行形态。网页入口、FastAPI、后台任务、CAD 执行和 IIS 代理之间需要更稳定地协作，尤其要避免字体预检、长任务和部署机环境异常拖垮 API 响应。这个阶段开始把任务队列、打包部署、诊断脚本和前端连接提示作为一个整体处理。

### 代表提交

- `c67c336`（2026-06-30）：稳定后端部署并补充图纸审查逻辑。
- `dd7ed8f`（2026-07-01）：feat: 升级翻版流程并增强后端连接自恢复。
- `4859fc9`（2026-07-05）：7.5提交整理，重新整合后端前端连接方式。
- `86c0aa4`（2026-07-05）：docs: design worker process queue architecture。
- `e2ce537`（2026-07-05）：docs: plan worker sqlite queue implementation。
- `a107733`（2026-07-07）：Stabilize packaged backend worker queue。
- `0907c5a`（2026-07-07）：Keep API responsive during font preflight。
- `835fc15`（2026-07-07）：Stabilize deployment runtime and CAD processing。
- `b51058d`（2026-07-08）：fix: keep job summaries stable for recent jobs。
- `de7bf6b`（2026-07-09）：fix: improve terminal deployment diagnostics。
- `451df0b`（2026-07-13）：fix: stabilize runtime connectivity and deployment diagnostics。

### 实际代码与文档变化

- `API/app/worker.py`、`API/app/runtime.py`、`backend/src/pipeline/sqlite_queue.py` 和 `group_manager.py` 建立后台 worker + SQLite 队列形态，让打包后的后端可以把长任务从 API 请求生命周期中拆出去。
- `API/app/routers/jobs.py` 调整任务创建、任务摘要和最近任务查询逻辑，避免字体预检或长任务导致接口阻塞，并让最近任务卡片在队列状态变化时保持稳定。
- `backend/src/deploy/terminal_package.py` 继续补齐部署包构建、启动入口、环境变量、第三份规范、前端静态资源和 worker 进程相关文件，减少开发目录与部署目录之间的差异。
- `tools/probe_target_env.ps1`、`tools/diagnose_iis_frontend_503.ps1`、`tools/compare_fanban_env.ps1` 增加 IIS、ARR、后端健康、服务入口、CAD/Office 资源和部署机差异检查，让“打不开页面”和“业务不能跑”可以分层定位。
- `backend/src/doc_gen/cover.py` 和 `spec_loader.py` 增加模板存在性、配置路径和封面生成诊断，部署缺文件时能给出更直接的错误。
- `frontend/src/app/App.tsx`、`httpAdapter.ts` 和相关测试调整后端连接提示、任务轮询和错误摘要，前端不再只按单次请求失败粗暴判断系统不可用。
- `docs/plans/2026-07-05-worker-process-and-sqlite-queue-design.md` 与 `2026-07-05-worker-process-and-sqlite-queue-implementation.md` 把 worker 队列设计和落地步骤文档化。

### 成果意义

这一版把项目从“开发机单进程可跑”继续推向“部署机长期运行”。worker 队列降低了长任务对 API 的占用，部署诊断脚本让 IIS/ARR、后端服务、CAD 运行时和业务健康可以拆开检查，前端连接提示也更接近真实状态。这对后续在 `D:\FanBanServer` 的实装验证非常关键。

### 当前能力边界

- 队列和诊断能力已经进入代码和部署包，但部署成功仍必须以目标机器 `D:\FanBanServer` 布局、服务入口和真实 DWG 任务为准，不能只用开发工作区测试替代。
- 前端健康提示已经更克制，但如果后端业务健康失败，仍应保留真实告警，不能把 CAD/Office/worker 故障包装成普通网络波动。

## 2026.07.31.1：CAD 出图、规范审查与字体兼容稳定版

### 阶段目标

7 月中下旬继续围绕真实图纸出图质量和规范审查做稳定性修复。图签中可能出现紧凑编码，标题中可能存在波浪线合并，图族输出、封面字体、灰度打印、异常 DWG 转换和规范审查兼容都需要在 CAD/.NET Bridge、Python 后端、YAML 和前端任务展示之间保持一致。

### 代表提交

- `00e71be`（2026-07-13）：feat: support compact internal codes and title tilde merge。
- `a40703a`（2026-07-15）：修复出图和纠错bug。
- `0e4e3d4`（2026-07-20）：完善CAD出图稳定性与终端部署配置。
- `388a02f`（2026-07-20）：chore: ignore local dependencies and QA artifacts。
- `35fc424`（2026-07-20）：fix: 修复图族输出与封面字体兼容。
- `a0d71e9`（2026-07-28）：feat: 完善字体兼容、失败诊断与灰度打印。
- `f46a138`（2026-07-29）：feat: 完善标准化出图与规范审查。
- `2164364`（2026-07-31）：fix: 修复规范审查兼容与异常DWG转换。

### 实际代码与文档变化

- `backend/src/cad/titleblock_extractor.py`、`titleblock_consistency.py` 和 `doc_gen/derivation.py` 支持紧凑内部编码、标题波浪线合并和图册基准码派生，减少图签格式变化导致的封面/目录编码错误。
- `backend/src/cad/a4_multipage.py`、`cad_dxf_executor.py` 和 `anchor_first_locator.py` 修复图族输出、窗口选择、锚点定位和拆图打印边界，让多页图纸和非标准图框处理更稳。
- .NET Bridge 的 `Commands.cs`、`PlotEngine.cs`、`AuditCheckScanner.cs`、`FontPreflightProcessor.cs` 继续补齐打印、扫描、字体预检和异常转换能力，并同步更新发布 DLL。
- `backend/src/cad/native_dxf_converter.py` 与 `DwgToDxfExporter.cs` 增加异常 DWG 转 DXF 的桥接兜底，`oda_converter.py` 也同步增强错误处理和回退策略。
- `backend/src/audit_check/standard_review.py`、`matcher.py`、`roi_mapper.py`、`models.py` 扩展标准化规范审查、禁词匹配、区域映射和报告字段，让规范审查不仅能扫到问题，也能更稳定地展示问题来源。
- `documents/参数规范.yaml`、`documents/参数规范_运行期.yaml`、`documents/参数规范-3.yaml` 继续补充审查、打印、字体和项目推断参数；`documents/Resources/fanban_monochrome-huidu.ctb` 新增灰度打印样式。
- 前端交付和翻版页面同步展示规范审查、上传推断、字体兼容和替换任务状态，避免后端新增能力后前端无法选择或无法解释结果。

### 成果意义

这一版解决的是“真实图纸不总是长得像标准样例”的问题。紧凑编码、标题合并、灰度打印、异常 DWG、字体缺失和规范审查结果都被纳入主链考虑，CAD 出图和纠错检查对现场图纸的适应性更强。

### 当前能力边界

- 异常 DWG 转换已经有 ODA 与 AutoCAD Bridge 兜底，但最终效果仍取决于目标机器上的 CAD 安装、可用版本和图纸本身是否损坏。
- 灰度打印和字体兼容进入配置后，新增打印样式仍需要同步检查 `documents/Resources`、运行期 YAML、部署包复制逻辑和前端选项。

## 2026.08.20.1：变更页码提取、编码对齐与通信打印样式版

### 阶段目标

8 月的重点是继续扩展交付链路的业务覆盖面。除常规出图、纠错和翻版外，程序开始支持归档资料中的变更页码提取，交付任务可选择图号对齐策略，同时补充通信专业打印样式和计算书模板资料。这个阶段的目标，是让任务能力更接近完整工程交付包。

### 代表提交

- `9077ebb`（2026-08-09）：纳入两个文件。
- `78e087b`（2026-08-12）：feat: 新增变更页码提取并完善字体打印兼容。
- `80f910d`（2026-08-19）：feat: add optional drawing code alignment。
- `2ab7a08`（2026-08-19）：refactor: move code alignment into task switches。
- `0cc94b5`（2026-08-20）：chore: ignore local validation screenshots。
- `fcd69f0`（2026-08-20）：feat: add telecom plot styles。

### 实际代码与文档变化

- `backend/src/change_page_extract` 新增变更页码提取服务，`API/app/runtime.py` 和 `API/app/routers/jobs.py` 将其接入任务创建、状态查询和下载路径。
- `backend/src/archive_tools/extractor.py` 与 `backend/src/deploy/archive_runtime.py` 增加归档资料解析和部署运行时准备能力，`tools/prepare_archive_runtime.py` 用于将相关归档运行资产纳入部署准备流程。
- `frontend/src/features/change-page-extract/ChangePageExtractWorkspace.tsx` 新增前端工作区，配合 `httpAdapter.ts`、`types.ts` 和任务展示逻辑，让变更页码提取成为可从页面触发和查看的任务。
- `backend/src/cad/titleblock_consistency.py`、`backend/src/models/doc_context.py`、`backend/src/pipeline/executor.py` 和前端交付任务开关新增可选图号对齐能力，允许在任务层决定是否执行图号一致性对齐，而不是把它固定为全局行为。
- `frontend/src/features/deliverable/taskDraft.ts`、`taskPresets.ts`、`schema.ts` 和 `DeliverableWorkspace.tsx` 将图号对齐移动到任务开关区域，和其它可选处理逻辑保持同一交互层级。
- `documents_bin/计算书模板文件.xlsx` 纳入业务模板资产；`documents/Resources/通信打印样式.ctb`、`通信打印样式细线.ctb` 和运行期 YAML 增加通信专业打印样式，部署构包和打印资源管理同步识别。
- `backend/src/cad/font_inventory.py`、`font_preflight.py`、`FontPreflightProcessor.cs` 继续补强字体打印兼容，避免新增任务类型后重新引入字体扫描或输出差异。

### 成果意义

这一版把项目从“出图和检查为主”继续扩展到“交付资料整理和任务级策略控制”。变更页码提取让归档资料处理进入系统，图号对齐改为任务开关让用户可以按实际图册选择策略，通信打印样式和计算书模板则扩大了专业和交付物覆盖范围。

### 当前能力边界

- 变更页码提取已经有后端服务和前端入口，但不同归档资料格式差异较大，后续仍应继续用真实归档包样本验证提取稳定性。
- 图号对齐已从固定行为调整为任务开关；后续若新增预设任务，应同步检查默认开关、前端草稿、后端参数和 YAML schema 是否保持一致。

## 2026.09.01.1：管理工作台、AI 图纸理解与对话接入同步版

### 阶段目标

本版同步补录管理前端和 AI 图纸理解工作树从 5 月底到 7 月 13 日的完整提交历史。阶段重点，是在既有 CAD 交付工作台外补齐账号、流程和工作量入口，同时建立独立 AI 参数规范、图纸理解基线、后端对话网关和终端连接诊断。

### Git 提交记录（22 项）

- `9ce0f52`（2026-05-29，feat: wire management workflow shell to yaml config）。
- `e2b827e`（2026-05-29，style: refine account and workload cockpit pages）。
- `52dcde1`（2026-05-29，feat: refine account management and workload cockpit）。
- `71ba9a9`（2026-06-02，feat: sync app shell workload visibility）。
- `0c5bc8b`（2026-06-03，修复前端下载）。
- `d1bb61d`（2026-06-15，Sync CAD plotting and split frontend modules）。
- `47e6a35`（2026-06-16，Add drawing understanding AI config baseline）。
- `f3bdb0c`（2026-06-16，Wire AI spec runtime config）。
- `7422b50`（2026-06-24，Group AI modules under AI directories）。
- `006bd56`（2026-06-24，Sync main parameter specs into AI worktree）。
- `f8857ad`（2026-07-09，Merge branch 'main' into codex/ai-drawing-understanding）。
- `2b64b56`（2026-07-12，Add backend AI chat gateway and terminal safeguards）。
- `7a7200f`（2026-07-12，Add floating AI chat drawer）。
- `ddae3b9`（2026-07-13，Harden AI connectivity diagnostics）。
- `a30250a`（2026-07-13，docs: clarify terminal deployment configuration checks）。
- `265d508`（2026-07-13，docs: design AI agent capability probe）。
- `6d6709f`（2026-07-13，docs: plan AI agent capability probe）。
- `4871d7d`（2026-07-13，Add comprehensive AI agent capability probe）。
- `cc05966`（2026-07-13，merge: sync main into frontend app shell）。
- `db0f2aa`（2026-07-13，merge: integrate AI drawing understanding）。
- `addb492`（2026-07-13，Fix AI connectivity probe runtime diagnostics）。
- `12eec01`（2026-07-13，fix: stabilize runtime connectivity and deployment diagnostics）。

### 版本更新

- `frontend` 管理模块开始按 YAML 能力配置接入账号、工作量和流程页面，并继续拆分前端模块、修复下载和可见性联动。
- `documents/AI/参数规范_AI.yaml`、AI 配置加载器和图纸理解工具建立第一版结构，使 AI 行为不只依赖代码默认值。
- 后端增加 AI 对话网关、会话存储和终端保护，前端增加浮动对话抽屉，形成首条可操作的 AI 交互链路。
- 模型能力探测、IIS/后端连接诊断和部署配置检查继续补齐，用于区分模型不可用、网关异常和终端部署问题。

### 成果意义

这一版使项目从单纯的 CAD 出图、纠错和翻版工作台，扩展为同时具备管理入口和 AI 图纸理解基础的统一应用。后续统一工作区和计算书 AI 的演进都以这一阶段的配置、网关和前端壳为基础。

### 当前能力边界

- `12eec01` 仍只保留在 `codex/ai-drawing-understanding`，没有进入当前 `main`；其余本版提交已经随计算书 AI 工作区合并进入主线。
- 本阶段 AI 能力仍以对话、探测和图纸理解基础为主，尚未包含多模态附件、离线规范库和计算书智能配筋。

## 2026.09.01.2：统一 AI 工作区、字体同步与多模态附件同步版

### 阶段目标

本版同步补录 7 月 14 日至 7 月 22 日的统一 AI 工作区、CAD 字体同步和多模态附件提交。阶段重点，是把分散的 AI 图纸理解与前端管理壳合并，补齐只读工具、安全 Markdown、离线规范基础，并让 AI 对话能够接收受控的图片、文档和 CAD 附件。

### Git 提交记录（30 项）

- `82c8409`（2026-04-27，更新）。
- `d6ce816`（2026-07-14，merge: sync latest main into unified workspace）。
- `0a134aa`（2026-07-14，merge: sync AI worktree into unified workspace）。
- `ad3096e`（2026-07-15，整合第一次修改）。
- `2325ab0`（2026-07-15，docs: plan single agent read-only access）。
- `522696f`（2026-07-20，fix: align account data columns）。
- `04363e9`（2026-07-20，feat: add read-only AI agent skills and diagnostics）。
- `324fbc9`（2026-07-20，fix: stabilize font preflight and AutoCAD inspection）。
- `2f15e5f`（2026-07-20，docs: design safe AI markdown rendering）。
- `596fb60`（2026-07-20，docs: plan safe AI markdown implementation）。
- `149d509`（2026-07-20，feat: add safe AI markdown renderer）。
- `6106f8e`（2026-07-20，feat: render assistant replies as markdown）。
- `79b15ff`（2026-07-20，feat: standardize AI markdown response format）。
- `09f019e`（2026-07-20，fix: keep AI markdown replies readable）。
- `717243a`（2026-07-20，docs: design offline building standards skill）。
- `97003b6`（2026-07-20，docs: plan offline building standards skill）。
- `f95ef51`（2026-07-20，feat: model standards acquisition audit）。
- `b72ef2b`（2026-07-20，feat: audit official standards sources）。
- `302ef32`（2026-07-20，docs: select standards validation corpus）。
- `a17b1f5`（2026-07-20，feat(ai): add offline building standards skill）。
- `5b7fecd`（2026-07-22，docs(ai): design attachment and multimodal support）。
- `1e4bb42`（2026-07-22，docs(ai): plan attachment implementation）。
- `f1f0536`（2026-07-22，feat(ai): secure proxy identity and configure attachments）。
- `23a2c6d`（2026-07-22，feat(ai): persist owner-isolated chat attachments）。
- `baaf62f`（2026-07-22，feat(ai): parse chat document and image attachments）。
- `3462cff`（2026-07-22，feat(ai): reuse drawing understanding for CAD attachments）。
- `655edf5`（2026-07-22，feat(ai): add attachment API and multimodal context）。
- `347d992`（2026-07-22，feat(ai): add attachment composer workflow）。
- `69f199a`（2026-07-22，fix(ai): keep failed attachments retriable）。
- `d0e538d`（2026-07-22，fix(ai): harden model capability probe）。

### 版本更新

- 统一工作区合并 AI 图纸理解和最新主线，整理账号数据字段，并增加只读 AI 工具、任务诊断和单代理访问边界。
- 前端增加安全 Markdown 渲染、统一回复格式和可读性修复，避免模型输出直接形成不受控页面内容。
- 离线建筑结构规范 Skill 建立来源审计、授权边界、验证语料和本地查询基础，为后续 15 本规范试点做准备。
- AI 附件链路增加所有者隔离存储、文档/图片解析、CAD 图纸理解复用、多模态上下文和失败附件重试。
- `codex/cad-font-sync` 独立形成字体同步服务、API、桌面启动入口和前端字体工作区，并强化字体预检与 AutoCAD 检查。

### 成果意义

这一版把 AI 从单一聊天入口扩展为有权限边界、有安全渲染、有附件上下文和有本地规范知识基础的工作区，同时保留独立 CAD 字体治理方案。

### 当前能力边界

- `82c8409`、`324fbc9` 仍只存在于锁定的 `codex/cad-font-sync`，没有进入当前 `main`；字体同步后续需要按模块选择性迁移。
- 离线规范能力在本阶段仍是基础框架，试点清单和 OCR 双通道证据分别属于后续版本和未提交工作区。

## 2026.09.01.3：计算书任务接入、规范授权与楼板流程同步版

### 阶段目标

本版同步补录 7 月 23 日至 7 月 30 日的计算书任务接入和统一 AI 合并记录。阶段重点，是把计算书从独立处理逻辑接入正式任务流，并在墙体计算基础上增加可选楼板应力、楼板图像识别和楼板配筋结果。

### Git 提交记录（14 项）

- `027ffd0`（2026-07-23，Merge branch 'main' into codex/calculation-book-integration）。
- `a0c9088`（2026-07-23，docs: design calculation book task integration）。
- `5ccb3c7`（2026-07-23，feat: integrate calculation book task workflow）。
- `15836c4`（2026-07-29，feat(ai): add authorized standards download tooling）。
- `d4c4c58`（2026-07-30，feat: harden calculation book preflight and exact reinforcement results）。
- `0abf419`（2026-07-30，merge: integrate unified AI workspace）。
- `d458855`（2026-07-30，merge: integrate calculation book workflow with AI workspace）。
- `df99d02`（2026-07-30，docs: design optional slab stress calculation）。
- `c122c82`（2026-07-30，docs: plan optional slab stress implementation）。
- `574895a`（2026-07-30，feat: bind optional slab stress to calculation preflight）。
- `14f10e7`（2026-07-30，feat: recognize slab figures in calculation archives）。
- `652bb82`（2026-07-30，feat: read slab reinforcement from calculation workbook）。
- `b442e2b`（2026-07-30，feat: render optional slab reinforcement results）。
- `f6e8d33`（2026-07-30，feat: expose optional slab stress workflow）。

### 版本更新

- 计算书任务接入后端任务模型、API、前端工作区和部署链路，预检进一步收紧并保留精确配筋结果。
- 统一 AI 工作区与计算书工作流合并，AI 网关、管理前端和计算书任务开始在同一分支共同演进。
- 授权规范下载工具进入代码，规范来源、授权状态和本地语料构建之间形成更明确的边界。
- 楼板应力被设计为可选流程，系统能够识别资料包中的楼板图、读取工作簿配筋并渲染楼板结果。

### 成果意义

这一版完成了计算书能力从后台模块到正式任务入口的迁移，并为后续 AI 钢筋表规范化建立了真实资料包、预检结果和墙板两类数据基础。

### 当前能力边界

- 楼板流程依赖输入资料中的图像和工作簿结构，缺少对应证据时应保持可选或进入复核，不得自动补造结果。
- 授权下载工具只负责受控来源获取和清单管理，不代表所有规范正文都可以直接纳入部署包。

## 2026.09.01.4：计算书 AI 配筋规范化、预设与模板同步版

### 阶段目标

本版同步补录 8 月 1 日至 8 月 4 日的计算书 AI 配筋规范化、任务预设和模板下载提交。阶段重点，是让模型输出接受确定性校验、来源行守恒和人工确认约束，并把高频计算书任务整理成可复用预设。

### Git 提交记录（44 项）

- `2c5e936`（2026-08-01，docs: design task AI reinforcement normalization）。
- `76a076b`（2026-08-01，docs: plan task AI reinforcement normalization）。
- `a3c8b83`（2026-08-01，feat: inspect calculation reinforcement workbook format）。
- `011f6bf`（2026-08-01，fix: keep workbook format inspection syntax-only）。
- `5b2aa82`（2026-08-01，fix: harden reinforcement workbook inspection）。
- `c6b6e82`（2026-08-01，fix: bound embedded XLSX resources）。
- `d1dbebf`（2026-08-01，feat: validate AI reinforcement fields deterministically）。
- `03fb692`（2026-08-01，fix: preserve resolved reinforcement review values）。
- `8b192d0`（2026-08-01，fix: enforce physical source row conservation）。
- `0a13ad8`（2026-08-01，feat: invoke reinforcement Skill in task worker）。
- `540ee95`（2026-08-01，fix: scope reinforcement task normalization）。
- `780c2c2`（2026-08-01，fix: harden reinforcement task normalization）。
- `16617da`（2026-08-01，fix: validate calculation AI env overrides）。
- `51332e6`（2026-08-01，feat: require confirmation before task AI normalization）。
- `4fe4f51`（2026-08-01，fix: preserve AI confirmation preflight token）。
- `c710dbe`（2026-08-01，fix: keep wall mismatch nonblocking and harden preflight）。
- `c4e0c46`（2026-08-01，fix: reject linked calculation preflight cache roots）。
- `f43eaaf`（2026-08-01，feat: normalize nonstandard reinforcement in calculation jobs）。
- `a81d41b`（2026-08-01，fix: preserve safe partial AI reinforcement results）。
- `7d4cb91`（2026-08-01，fix: bind AI normalization to trusted workbook row count）。
- `1361232`（2026-08-01，feat: leave ambiguous calculation reinforcement blank）。
- `f71fd10`（2026-08-01，feat: expose calculation reinforcement warnings）。
- `5c42910`（2026-08-01，feat: show AI reinforcement warnings in task details）。
- `08b293d`（2026-08-01，fix: gate calculation AI completion warnings）。
- `fe6faed`（2026-08-01，fix: handle partial calculation preflight evidence）。
- `e55e9ed`（2026-08-01，feat: support real calculation archives and row audits）。
- `3058d2a`（2026-08-01，fix: allow slab image groups without reinforcement rows）。
- `aeab855`（2026-08-01，test: cover calculation AI normalization workflow）。
- `e2a34d8`（2026-08-02，fix: audit reinforcement rows without replaying full tables）。
- `f68e609`（2026-08-02，fix: keep narrow task actions below the header）。
- `4b2ced8`（2026-08-03，docs: design compact calculation book presets）。
- `63839c5`（2026-08-03，docs: plan compact calculation book presets）。
- `a31c695`（2026-08-03，feat: add calculation book presets）。
- `1d42009`（2026-08-03，feat: manage calculation book presets）。
- `53d6fc6`（2026-08-03，style: compact calculation book task setup）。
- `c8c03ca`（2026-08-03，fix: refine calculation book responsive layout）。
- `7b8cb55`（2026-08-04，Merge branch 'main' into codex/calculation-ai-unified）。
- `2c91107`（2026-08-04，chore: update calculation book templates）。
- `be47e45`（2026-08-04，feat: add standard reinforcement template download）。
- `c5cfc73`（2026-08-04，feat: add reinforcement template download button）。
- `4d3e58f`（2026-08-04，test: cover isolated template download state）。
- `eb21438`（2026-08-04，fix: harden template download feedback）。
- `68054e3`（2026-08-04，fix: reject impossible reinforcement layer counts）。
- `a894de7`（2026-08-04，style: improve calculation preset touch targets）。

### 版本更新

- 钢筋工作簿检查增加格式识别、嵌入资源限制和来源单元格证据，避免异常 XLSX 或链接缓存进入任务链。
- AI 配筋字段增加确定性校验、物理来源行守恒、可信工作簿行数绑定和歧义留空规则，模型不能直接覆盖缺少证据的值。
- 任务 worker 正式调用配筋 Skill，并增加预检确认令牌、环境变量校验、部分安全结果保留和复核告警展示。
- 真实计算资料包和逐行审查进入测试范围，墙体不匹配保持非阻断，楼板图片组也能在缺少配筋行时给出明确状态。
- 前端增加计算书预设管理、标准配筋模板下载、紧凑任务配置和响应式布局。

### 成果意义

这一版把 AI 配筋从普通模型调用收紧为有格式、有证据、有确认、有守恒规则的工程任务，并通过预设和模板降低重复配置成本。

### 当前能力边界

- AI 规范化只能在确定性预检和证据校验通过后使用；歧义项必须留空或转人工复核。
- 模板下载和预设已经进入任务界面，但真实项目仍需核对模板字段、YAML 参数和计算书类型是否一致。

## 2026.09.01.5：SMX 智能配筋与计算书生成同步版

### 阶段目标

本版同步补录 8 月 5 日至 8 月 6 日的 SMX 智能配筋、计算书渲染和诊断提交。阶段重点，是从资料来源预检开始，生成受约束的 SMX 配筋候选，通过内部模型 Skill 给出建议，并把结果写入正式计算书。

### Git 提交记录（21 项）

- `4a972cc`（2026-08-05，docs: design AI rebar suggestions from SMX）。
- `7caa8e5`（2026-08-05，docs: plan AI rebar calculation workflow）。
- `c404462`（2026-08-05，feat: add explicit calculation reinforcement source）。
- `59c2824`（2026-08-05，feat: preflight calculation archives by rebar source）。
- `9fa9fdc`（2026-08-06，feat: generate exact SMX rebar candidates）。
- `342a67e`（2026-08-06，feat: validate structured SMX rebar selections）。
- `6aafda9`（2026-08-06，feat: add generic SMX rebar recommendation skill）。
- `6ebfcbc`（2026-08-06，feat: call SMX recommendation skill through internal model）。
- `50cc058`（2026-08-06，feat: converge invalid AI rebar selections）。
- `c6b51bc`（2026-08-06，feat: add calculation task diagnostic logs）。
- `b99e081`（2026-08-06，feat: transport AI calculation rebar mode）。
- `017504b`（2026-08-06，feat: add image-only AI calculation workflow）。
- `0e4f365`（2026-08-06，feat: present AI calculation results and logs）。
- `1b94ebe`（2026-08-06，feat: render AI rebar calculation books）。
- `ca633dc`（2026-08-06，feat: expose AI calculation diagnostics）。
- `08e9b9b`（2026-08-06，feat: package and diagnose AI rebar skill）。
- `783face`（2026-08-06，test: verify AI rebar calculation workflow）。
- `9fcfe37`（2026-08-06，fix: prioritize calculation review warnings）。
- `af51996`（2026-08-06，fix: harden AI calculation execution and diagnostics）。
- `13ae889`（2026-08-06，docs: define build artifact retention policy）。
- `ba02640`（2026-08-06，chore: clean generated build artifacts）。

### 版本更新

- 计算资料包增加明确的配筋来源类型，预检按来源选择检查规则，避免把图片、表格和已有配筋结果混为同一种输入。
- 系统能够生成精确 SMX 候选、校验结构化选择、调用通用推荐 Skill，并收敛无效模型选择。
- 任务支持仅图片的 AI 计算流程、配筋模式传输、结果日志展示和正式计算书渲染。
- 后端增加任务诊断日志、AI 计算诊断接口和 Skill 打包检查，前端优先展示需要人工关注的复核告警。
- 构建产物保留策略和清理提交同步进入分支，减少生成文件污染后续部署包。

### 成果意义

这一版把 AI 配筋建议接入实际计算书生成链路，同时保留候选约束、结构校验、诊断日志和复核警告，使结果具备更清楚的来源和失败解释。

### 当前能力边界

- SMX 推荐属于受约束的工程辅助结果，不能替代设计人员对荷载、构造和规范适用性的最终判断。
- 真实计算书生成仍依赖 Word/Excel 模板和 Office 环境，部署验证不能只依赖单元测试。

## 2026.09.01.6：任务组管理闭环与计算资料归档同步版

### 阶段目标

本版同步补录 8 月 7 日至 8 月 8 日的任务组管理、工作量、账号页面和计算资料归档提交。阶段重点，是把任务提交、审批、归档和工作量结算闭合，同时安全支持 ZIP、RAR 和 7Z 计算资料包。

### Git 提交记录（32 项）

- `e00e60d`（2026-08-07，docs: plan workload lifecycle and account UI refresh）。
- `998c9be`（2026-08-07，fix: guard task group workflow submission）。
- `4d08e1c`（2026-08-07，fix: harden task group submission readiness）。
- `c6f5f15`（2026-08-07，fix: tighten task group readiness policy）。
- `df5ef57`（2026-08-07，docs: design compact calculation wall review）。
- `eb7914f`（2026-08-07，fix: close task group archive and workload lifecycle）。
- `7f9edb4`（2026-08-07，feat: compact calculation wall review）。
- `7a19358`（2026-08-07，feat: add task group workflow panel）。
- `9891ce1`（2026-08-07，feat: redesign workload control center）。
- `038d9c7`（2026-08-07，fix: harden task group workflow interactions）。
- `7cb0342`（2026-08-07，fix: correct workload projections and workflow states）。
- `7f0d2a8`（2026-08-07，docs: design secure calculation archive support）。
- `aa1084a`（2026-08-07，docs: plan calculation archive format support）。
- `47fa39b`（2026-08-07，feat: redesign account management workspace）。
- `897ebfd`（2026-08-07，feat: configure calculation archive formats）。
- `e2fad0b`（2026-08-07，fix: enforce calculation archive signature validation）。
- `e461278`（2026-08-07，feat: safely extract rar and 7z calculation archives）。
- `e0840e6`（2026-08-07，fix: sanitize external archive command paths）。
- `192e55c`（2026-08-07，fix: make task group lifecycle durable across processes）。
- `e2921f3`（2026-08-07，fix: harden calculation archive preflight limits）。
- `23ec7f9`（2026-08-08，fix: align workload totals and repair safeguards）。
- `4725578`（2026-08-08，feat: wire calculation archive extractor runtime）。
- `7d93422`（2026-08-08，fix: secure account APIs and management interactions）。
- `5870c77`（2026-08-08，feat: expose 7z calculation uploads）。
- `33d91b3`（2026-08-08，fix: parse 7zip listing flags safely）。
- `849486d`（2026-08-08，fix: make session persistence atomic）。
- `b11f3ac`（2026-08-08，feat: package verified portable 7zip runtime）。
- `f5e2984`（2026-08-08，fix: harden portable archive runtime packaging）。
- `eac93c7`（2026-08-08，feat: add fail-closed archive runtime probe）。
- `cfefea1`（2026-08-08，docs: design dev launch and AI tab update）。
- `85abdee`（2026-08-08，docs: plan dev launch and AI tab update）。
- `11b1b10`（2026-08-08，merge: integrate calculation archive formats）。

### 版本更新

- 任务组提交增加冲突检查、完整性门禁和 readiness 策略，流程卡片与任务状态交互进一步收紧。
- 前端补齐任务组流程面板、紧凑计算墙复核、账号管理工作区和工作量控制中心，管理模块不再只是占位入口。
- 任务组生命周期改为跨进程持久化，归档成功与工作量结算继续形成闭环，账号 API 和会话写入同步加强。
- 计算资料归档增加格式配置、文件签名校验、外部命令路径清理、解压限额和 RAR/7Z 安全提取。
- 部署包加入经过校验的便携式 7-Zip 运行时、7Z 上传入口和失败关闭的归档运行时探测。

### 成果意义

这一版把计算书任务从单次处理扩展为可提交、可审批、可归档、可结算的管理闭环，并解决多种压缩格式在部署机上的运行时依赖问题。

### 当前能力边界

- 归档成功仍是最终工作量结算触发点，流程状态和归档目录不能由前端自行推断。
- 便携式归档运行时必须随正式部署包校验，外部命令失败不能被解释为空资料包。

## 2026.09.01.7：部署业务探测与 AI 入口完善同步版

### 阶段目标

本版同步补录 8 月 9 日至 8 月 10 日的部署业务探测、AI 入口和计算书诊断提交。阶段重点，是让终端探测不只确认进程存在，还能验证账号、工作量、计算书、Office COM、worker 和 Skill 是否真的可用。

### Git 提交记录（21 项）

- `e5c38b6`（2026-08-09，docs: design deployment business probes）。
- `312c8d9`（2026-08-09，docs: plan deployment business probes）。
- `a9681e7`（2026-08-09，fix: bound Office COM deployment probes）。
- `002b547`（2026-08-09，fix: require live worker in deployment health）。
- `a77fb90`（2026-08-09，feat: add redacted deployment probe reports）。
- `38b7a74`（2026-08-09，docs: design AI mascot trigger）。
- `3bca68b`（2026-08-09，docs: plan AI mascot trigger implementation）。
- `b630eda`（2026-08-09，feat: replace AI tab with animated mascot）。
- `b20d180`（2026-08-09，docs: design AI drawer mascot handle）。
- `85b9fbf`（2026-08-09，docs: plan AI drawer mascot handle）。
- `14c4d33`（2026-08-09，feat: resize AI drawer from mascot handle）。
- `224d5f6`（2026-08-10，feat: probe account and workload health）。
- `598f08b`（2026-08-10，feat: probe calculation book deployment health）。
- `4edf283`（2026-08-10，feat: package deployment business probes）。
- `bef4fd1`（2026-08-10，docs: streamline terminal installation commands）。
- `dec6ec1`（2026-08-10，fix: align calculation probe with packaged skills）。
- `5e53944`（2026-08-10，docs: design direct terminal overlay validation）。
- `449272a`（2026-08-10，docs: plan terminal overlay validation implementation）。
- `86c4f21`（2026-08-10，fix: show all calculation book parameter errors）。
- `c0d585a`（2026-08-10，docs: record AI rebar skill evolution research）。
- `eb7efe8`（2026-08-10，docs: clean AI rebar research formatting）。

### 版本更新

- 部署探测增加 Office COM 时间边界、活动 worker 要求和脱敏报告，避免把进程存活误判为业务健康。
- 账号、工作量和计算书业务探测被打包进终端部署包，计算书探测与实际随包 Skill 保持一致。
- 前端 AI 入口由固定页签改为动画触发图标，并支持从图标拖动调整对话抽屉尺寸。
- 终端安装命令、界面覆盖验证方案和计算书参数错误汇总同步完善。
- AI 配筋 Skill 演进研究和格式整理进入文档，为后续模型及规则升级保留设计依据。

### 成果意义

这一版让部署验收从端口和进程检查深入到实际业务能力，同时让 AI 入口更加独立，不再挤占既有业务模块页签。

### 当前能力边界

- 业务探测仍需分别报告后端可达、业务健康、IIS/ARR 和 worker 状态，不能用单一绿色状态覆盖真实故障。
- AI 入口调整只改变交互方式，不改变后端权限、模型连接和会话隔离要求。

## 2026.09.01.8：主线同步、翻版稳定与离线规范试点版

### 阶段目标

本版同步补录 8 月 12 日至 8 月 21 日的主线同步、翻版纠错、终端部署和离线规范试点提交。阶段重点，是把计算书分支重新对齐当时主线，修复真实翻版与单位换算问题，并确定 15 本离线规范的试点范围。

### Git 提交记录（11 项）

- `cf7a1fd`（2026-08-12，docs: checkpoint main synchronization plan）。
- `717e034`（2026-08-12，merge: integrate latest main into calculation AI workspace）。
- `05a7a7d`（2026-08-13，fix: 完善翻版图签与编码保护逻辑）。
- `212b09f`（2026-08-13，feat: 生成并校验终端部署压缩包）。
- `21f04b2`（2026-08-18，fix(audit-replace): harden unit conversion and smoke stability）。
- `73e13a4`（2026-08-19，feat: add optional drawing code alignment）。
- `5f36b02`（2026-08-19，refactor: move code alignment into task switches）。
- `c2f017b`（2026-08-20，chore: ignore local validation screenshots）。
- `aafe2a4`（2026-08-20，feat: add telecom plot styles）。
- `443b447`（2026-08-21，docs(ai): plan 15-standard offline skill pilot）。
- `6da40c0`（2026-08-21，feat(ai): define 15-standard pilot manifest）。

### 版本更新

- 计算书 AI 分支重新合并最新主线，并记录同步检查点，减少 CAD 主链与计算书分支继续分叉。
- 翻版图签和编码保护逻辑继续收紧，纠错替换中的单位换算和真实烟测稳定性得到专项修复。
- 终端部署压缩包生成和校验进入提交，任务级图号对齐开关及通信打印样式同步保留在工作树历史中。
- 离线建筑结构规范 Skill 明确 15 本试点清单和离线构建计划，为后续 OCR、查询证据和验证集升级提供固定基线。

### 成果意义

这一版把计算书 AI 分支重新拉回 CAD 业务主线，并同时固定真实翻版、部署包和离线规范试点三个后续整合基准。

### 当前能力边界

- `443b447`、`6da40c0` 仍只存在于 `codex/unified-workspace`，尚未进入当前 `main`。
- 统一工作区现有 OCR、双通道证据和试点验证脚本仍是未提交改动，不属于本版正式提交记录。

## 2026.09.01.9：Qwen3.8、计算书封面编码与主线整合版

### 阶段目标

本版记录 8 月 31 日至 9 月 1 日的最新正式整合。阶段重点，是切换终端 AI 网关默认模型，完成计算书 AI 输出容错与封面编码写入，修复主图图签字段继承，并把计算书 AI 统一工作区整体合并到 `main`。

### Git 提交记录（6 项）

- `2bc70c8`（2026-08-31，docs: plan calculation book AI and cover code）。
- `f71a8ed`（2026-08-31，docs: plan terminal Qwen3.8 model switch）。
- `780814f`（2026-08-31，feat: switch terminal gateway to Qwen3.8）。
- `a2bd8ba`（2026-09-01，feat: complete calculation book AI normalization and cover code）。
- `4b1f650`（2026-09-01，fix: preserve partial primary titleblock metadata）。
- `206e7c5`（2026-09-01，merge: integrate calculation AI unified workspace）。

### 版本更新

- 终端 AI 网关默认模型切换为 Qwen3.8，并保留模型切换方案和终端配置说明。
- 计算书 AI 规范化增加模型 JSON 提取、结构清理和有限自纠重试，封面编码、模板、YAML 和前端 schema 同步完成。
- 主图选择在 `-001` 图框字段不完整时保留已经提取到的工程信息，避免错误继承后续图页的项目、子项或专业字段。
- `206e7c5` 将 `codex/calculation-ai-unified` 合并进 `main`；当前计算书分支 HEAD `a2bd8ba` 已成为主线祖先，`codex/python313-crash-lab` 保留的 Python 3.13 稳定快照 `21f04b2` 也随之进入主线。
- AI 连接诊断脚本改用 .NET SHA256 实现，不再依赖旧版 Windows PowerShell 环境中可能无法自动加载的 `Get-FileHash`。
- 本次更新日志按当前 Git 拓扑补录 201 个提交：196 个已进入 `main`，5 个仍保留在 AI 图纸理解、CAD 字体同步和统一工作区。

### 成果意义

这一版是其他 worktree 大规模成果进入主线的正式节点。计算书 AI、管理闭环、AI 对话、多模态附件、归档运行时和部署业务探测不再只是独立工作树实验，而是进入当前主线历史。

### 当前能力边界

- `codex/ai-drawing-understanding` 仍保留 `12eec01`，`codex/cad-font-sync` 仍保留 `82c8409`、`324fbc9`，`codex/unified-workspace` 仍保留 `443b447`、`6da40c0`，这 5 个提交尚未并入 `main`。
- `codex/unified-workspace` 仍有 17 个已跟踪文件和 6 个未跟踪文件入口，主要涉及 OCR 和规范试点；这些在研改动没有写入正式提交清单。
